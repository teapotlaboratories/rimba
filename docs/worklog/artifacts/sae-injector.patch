diff --git a/src/common/sae.c b/src/common/sae.c
index 358485588..65db3a2ec 100644
--- a/src/common/sae.c
+++ b/src/common/sae.c
@@ -1691,6 +1691,15 @@ int sae_write_commit(struct sae_data *sae, struct wpabuf *buf,
 		return -1;
 	wpa_hexdump(MSG_DEBUG, "SAE: own commit-scalar",
 		    pos, sae->tmp->prime_len);
+#ifdef CONFIG_TESTING_OPTIONS
+	if (sae->test_corrupt_commit == SAE_CORRUPT_SCALAR_ZERO) {
+		os_memset(pos, 0, sae->tmp->prime_len);
+		wpa_printf(MSG_INFO, "SAE-ATTACK: commit-scalar zeroed");
+	} else if (sae->test_corrupt_commit == SAE_CORRUPT_SCALAR_GE_R) {
+		os_memset(pos, 0xff, sae->tmp->prime_len);
+		wpa_printf(MSG_INFO, "SAE-ATTACK: commit-scalar set 0xff.. (>= r)");
+	}
+#endif /* CONFIG_TESTING_OPTIONS */
 	if (sae->tmp->ec) {
 		pos = wpabuf_put(buf, 2 * sae->tmp->prime_len);
 		if (crypto_ec_point_to_bin(sae->tmp->ec,
@@ -1701,6 +1710,12 @@ int sae_write_commit(struct sae_data *sae, struct wpabuf *buf,
 			    pos, sae->tmp->prime_len);
 		wpa_hexdump(MSG_DEBUG, "SAE: own commit-element(y)",
 			    pos + sae->tmp->prime_len, sae->tmp->prime_len);
+#ifdef CONFIG_TESTING_OPTIONS
+		if (sae->test_corrupt_commit == SAE_CORRUPT_ELEM_OFFCURVE) {
+			pos[sae->tmp->prime_len - 1] ^= 0x01;
+			wpa_printf(MSG_INFO, "SAE-ATTACK: commit-element perturbed off-curve");
+		}
+#endif /* CONFIG_TESTING_OPTIONS */
 	} else {
 		pos = wpabuf_put(buf, sae->tmp->prime_len);
 		if (crypto_bignum_to_bin(sae->tmp->own_commit_element_ffc, pos,
diff --git a/src/common/sae.h b/src/common/sae.h
index c3ca4d41b..2ce428817 100644
--- a/src/common/sae.h
+++ b/src/common/sae.h
@@ -104,6 +104,15 @@ enum sae_state {
 	SAE_NOTHING, SAE_COMMITTED, SAE_CONFIRMED, SAE_ACCEPTED
 };
 
+#ifdef CONFIG_TESTING_OPTIONS
+enum sae_test_corrupt {
+	SAE_CORRUPT_NONE = 0,
+	SAE_CORRUPT_SCALAR_ZERO = 1,
+	SAE_CORRUPT_SCALAR_GE_R = 2,
+	SAE_CORRUPT_ELEM_OFFCURVE = 3
+};
+#endif /* CONFIG_TESTING_OPTIONS */
+
 struct sae_data {
 	enum sae_state state;
 	u16 send_confirm;
@@ -120,6 +129,9 @@ struct sae_data {
 	u16 rc; /* protocol instance variable: Rc (received send-confirm) */
 	unsigned int h2e:1;
 	unsigned int pk:1;
+#ifdef CONFIG_TESTING_OPTIONS
+	unsigned int test_corrupt_commit:2;
+#endif /* CONFIG_TESTING_OPTIONS */
 	struct sae_temporary_data *tmp;
 };
 
diff --git a/wpa_supplicant/ctrl_iface.c b/wpa_supplicant/ctrl_iface.c
index 01d596819..af7c02df3 100644
--- a/wpa_supplicant/ctrl_iface.c
+++ b/wpa_supplicant/ctrl_iface.c
@@ -20,6 +20,7 @@
 #include "common/ieee802_11_defs.h"
 #include "common/ieee802_11_common.h"
 #include "common/wpa_ctrl.h"
+#include "common/sae.h"
 #ifdef CONFIG_DPP
 #include "common/dpp.h"
 #endif /* CONFIG_DPP */
@@ -58,6 +59,7 @@
 #include "offchannel.h"
 #include "drivers/driver.h"
 #include "mesh.h"
+#include "mesh_rsn.h"
 #include "dpp_supplicant.h"
 #include "sme.h"
 #include "nan_usd.h"
@@ -12903,6 +12905,19 @@ char * wpa_supplicant_ctrl_iface_process(struct wpa_supplicant *wpa_s,
 	} else if (os_strncmp(buf, "MESH_LINK_PROBE ", 16) == 0) {
 		if (wpa_supplicant_ctrl_iface_mesh_link_probe(wpa_s, buf + 16))
 			reply_len = -1;
+#ifdef CONFIG_TESTING_OPTIONS
+	} else if (os_strncmp(buf, "MESH_ATTACK malformed-commit ", 29) == 0) {
+		u8 _mac[ETH_ALEN];
+		int _mode = SAE_CORRUPT_SCALAR_GE_R;
+		char *_p = buf + 29;
+		if (os_strstr(_p, "--scalar-zero"))
+			_mode = SAE_CORRUPT_SCALAR_ZERO;
+		else if (os_strstr(_p, "--elem-offcurve"))
+			_mode = SAE_CORRUPT_ELEM_OFFCURVE;
+		if (hwaddr_aton(_p, _mac) ||
+		    mesh_attack_send_commit(wpa_s, _mac, _mode))
+			reply_len = -1;
+#endif /* CONFIG_TESTING_OPTIONS */
 #endif /* CONFIG_MESH */
 #ifdef CONFIG_P2P
 	} else if (os_strncmp(buf, "P2P_FIND ", 9) == 0) {
diff --git a/wpa_supplicant/mesh_rsn.c b/wpa_supplicant/mesh_rsn.c
index 5c7a6c49b..2813a25ec 100644
--- a/wpa_supplicant/mesh_rsn.c
+++ b/wpa_supplicant/mesh_rsn.c
@@ -431,6 +431,32 @@ int mesh_rsn_auth_sae_sta(struct wpa_supplicant *wpa_s,
 	return 0;
 }
 
+#ifdef CONFIG_TESTING_OPTIONS
+int mesh_attack_send_commit(struct wpa_supplicant *wpa_s, const u8 *addr,
+			    int mode)
+{
+	struct hostapd_data *hapd = wpa_s->ifmsh ? wpa_s->ifmsh->bss[0] : NULL;
+	struct wpa_ssid *ssid = wpa_s->current_ssid;
+	struct sta_info *sta;
+
+	if (!hapd || !ssid)
+		return -1;
+	sta = ap_get_sta(hapd, addr);
+	if (!sta || !sta->sae) {
+		wpa_printf(MSG_INFO, "SAE-ATTACK: no SAE peer " MACSTR,
+			   MAC2STR(addr));
+		return -1;
+	}
+	if (mesh_rsn_build_sae_commit(wpa_s, ssid, sta))
+		return -1;
+	sta->sae->test_corrupt_commit = mode;
+	wpa_printf(MSG_INFO, "SAE-ATTACK: sending malformed Commit mode=%d to "
+		   MACSTR, mode, MAC2STR(addr));
+	auth_sae_init_committed(hapd, sta);
+	return 0;
+}
+#endif /* CONFIG_TESTING_OPTIONS */
+
 
 void mesh_rsn_get_pmkid(struct mesh_rsn *rsn, struct sta_info *sta, u8 *pmkid)
 {
diff --git a/wpa_supplicant/mesh_rsn.h b/wpa_supplicant/mesh_rsn.h
index 8775cedc3..49679fdf0 100644
--- a/wpa_supplicant/mesh_rsn.h
+++ b/wpa_supplicant/mesh_rsn.h
@@ -42,4 +42,9 @@ int mesh_rsn_process_ampe(struct wpa_supplicant *wpa_s, struct sta_info *sta,
 			  const u8 *start, size_t elems_len);
 void mesh_auth_timer(void *eloop_ctx, void *user_data);
 
+#ifdef CONFIG_TESTING_OPTIONS
+int mesh_attack_send_commit(struct wpa_supplicant *wpa_s, const u8 *addr,
+			    int mode);
+#endif /* CONFIG_TESTING_OPTIONS */
+
 #endif /* MESH_RSN_H */
