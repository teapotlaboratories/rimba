# Worklog HTML

Hand-authored HTML renders of the worklogs in `../` (`*.md`). Open `index.html`.

Each page mirrors one worklog with diagrams/visuals drawn from its content
(flow diagrams, before/after bars, stat grids, callouts); all pages share
`style.css` and support a light/dark theme + a per-page table of contents.
