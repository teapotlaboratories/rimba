# Morse components are vendored (EXTRA_COMPONENT_DIRS). iperf-cmd + ping-cmd provide the
# `iperf -s` / `ping` serial-console commands used by this throughput example.
dependencies:
  idf: ">=5.4.2,<6.0"
  espressif/iperf-cmd: "~0.1.1"
  esp-qa/ping-cmd: "~1.0.0"
