# Morse components are vendored (EXTRA_COMPONENT_DIRS). The esp_console + iperf-cmd
# components provide the serial `iperf` / `ping` commands this example drives.
dependencies:
  idf: ">=5.4.2,<6.0"
  espressif/iperf-cmd: "~0.1.1"
  esp-qa/ping-cmd: "~1.0.0"
